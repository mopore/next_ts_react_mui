version: "3"

services:
  my-next-app:
    container_name: my-next-app
    image: my-next-app
    ports:
      - 80:80
    restart: unless-stopped 
    deploy:
      resources:
        limits:
          memory: 500m